name: zero_flutter
description: A new Flutter package project.

environment:
  sdk: ">=2.18.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Routing
  go_router: ^6.5.0

  # Riverpod + Hooks (state)
  flutter_riverpod: ^2.3.2
  riverpod_annotation: ^2.0.2
  flutter_hooks: ^0.18.6
  hooks_riverpod: ^2.3.2

  # Utils
  url_launcher: ^6.1.10

  # Localization
  slang: ^3.14.0
  slang_flutter: ^3.14.0
  intl: ^0.17.0

  # Authentication
  crypto: ^3.0.2
  google_sign_in: ^6.0.2
  sign_in_with_apple: ^4.3.0

  # User Interface
  flutter_svg: ^2.0.4
  smooth_corner: ^1.1.0

  # Images
  camera: ^0.10.3+2
  image_picker: ^0.8.7
  extended_image: ^7.0.2
  image: ^4.0.15

  # Firebase
  cloud_firestore_odm: ^1.0.0-dev.52
  json_annotation: ^4.8.0
  firebase_core: ^2.8.0
  firebase_auth: ^4.4.0
  firebase_auth_web: ^5.3.0
  cloud_functions: ^4.0.13
  cloud_firestore: ^4.4.5
  firebase_remote_config: ^4.0.0

dev_dependencies:
  test: ^1.22.0
  flutter_test:
    sdk: flutter
  onezero_flutter_lints:
    git:
      url: https://github.com/onezerocompany/coding.git
      path: packages/onezero_flutter_lints
  build_runner: ^2.3.0
  slang_build_runner: ^3.14.0
  riverpod_generator: ^2.1.4
  custom_lint: ^0.3.2
  riverpod_lint: ^1.1.6
  cloud_firestore_odm_generator: ^1.0.0-dev.52
  json_serializable: ^6.6.1

flutter:
  uses-material-design: false
  fonts:
    - family: MaterialSymbolsRounded
      fonts:
        - asset: assets/fonts/material-symbols-rounded.ttf
  assets:
    - assets/icons/
    - assets/fonts/
    - assets/flags/
