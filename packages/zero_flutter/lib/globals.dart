import 'package:flutter/animation.dart';

const double glassTransparency = 0.85;
const double glassBlur = 20;
const double smoothness = 0.8;
const Duration pageTransitionDuration = Duration(milliseconds: 550);
const Curve pageTransitionCurve = Curves.fastOutSlowIn;
