import { addons, useParameter } from "@storybook/addons";
import { GLOBAL_KEY, PARAM_KEY, THEMING_EVENTS, DEFAULT_THEME_PARAMETERS } from "../constants";
/**
 *
 * @param StoryContext
 * @returns The global theme name set for your stories
 */

export function pluckThemeFromContext(_ref) {
  var globals = _ref.globals;
  return globals[GLOBAL_KEY] || "";
}
export function useThemeParameters() {
  return useParameter(PARAM_KEY, DEFAULT_THEME_PARAMETERS);
}
export function initializeThemeState(themeNames, defaultTheme) {
  addons.getChannel().emit(THEMING_EVENTS.REGISTER_THEMES, {
    defaultTheme: defaultTheme,
    themes: themeNames
  });
}