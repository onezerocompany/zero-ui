"use strict";

var _addons = require("@storybook/addons");

var _constants = require("../constants");

var _themeSwitcher = require("../components/theme-switcher");

// Register the addon
_addons.addons.register(_constants.ADDON_ID, function (api) {
  // Register the theme switcher
  _addons.addons.add(_constants.THEME_SWITCHER_ID, {
    type: _addons.types.TOOL,
    title: "Theme",
    match: function match(_ref) {
      var viewMode = _ref.viewMode;
      return !!(viewMode && viewMode.match(/^(story|docs)$/));
    },
    render: _themeSwitcher.ThemeSwitcher,
    paramKey: _constants.PARAM_KEY
  });
});