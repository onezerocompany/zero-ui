import { DecoratorFunction } from "@storybook/addons";
declare type Theme = Record<string, any>;
declare type ThemeMap = Record<string, Theme>;
export interface ProviderStrategyConfiguration {
    Provider?: any;
    GlobalStyles?: any;
    defaultTheme?: string;
    themes?: ThemeMap;
}
export declare const withThemeFromJSXProvider: ({ Provider, GlobalStyles, defaultTheme, themes, }: ProviderStrategyConfiguration) => DecoratorFunction;
export {};
