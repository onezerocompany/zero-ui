{
  "workspaces": [
    "packages/zero-ui",
    "previews/web"
  ],
  "scripts": {
    "build": "npm run -ws build"
  },
  "prettier": "@onezerocompany/prettier-config",
  "devDependencies": {
    "@onezerocompany/prettier-config": "^1.41.0"
  }
}
